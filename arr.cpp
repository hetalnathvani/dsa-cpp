#include <iostream>
#include <math.h>

using namespace std;

int main() {
    int third[3] = {5, 7, 11};

    int thirdSize = sizeof(third) / sizeof(int);
    cout << "Size of Third is : " << thirdSize << endl;

    char ch[5] = {'a', 'b', 'c', 'd', 'e'};
}
